const TodosForm = () => {
  return (
    <form>
      <input type="text" placeholder="Add Task" />
      <div>
        <button>Add Task</button>
        <button>Clear</button>
      </div>
    </form>
  );
};

export default TodosForm;
